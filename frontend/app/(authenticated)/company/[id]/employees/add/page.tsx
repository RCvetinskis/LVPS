import AddEmploye from "../_components/AddEmploye";

type Props = {};

const AddEmployePage = (props: Props) => {
  return (
    <div className=" w-full flex items-center justify-center">
      <AddEmploye />
    </div>
  );
};

export default AddEmployePage;
